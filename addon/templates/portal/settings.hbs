<div class="portal-content-wrapper">
    <div class="portal-settings-header">
        <div>
            <div class="portal-section-eyebrow">{{t "portal.settings.eyebrow"}}</div>
            <h1 class="text-lg font-bold">{{t "portal.settings.title"}}</h1>
        </div>
    </div>

    <div class="portal-settings-tabs">
        <LinkTo @route="portal.settings.account" class="portal-settings-tab">{{t "portal.settings.tabs.account"}}</LinkTo>
        <LinkTo @route="portal.settings.members" class="portal-settings-tab">{{t "portal.settings.tabs.members"}}</LinkTo>
    </div>

    {{outlet}}
</div>
